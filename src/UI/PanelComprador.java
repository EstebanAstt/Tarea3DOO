package UI;

import java.awt.*;

public class PanelComprador {
    public void paintComponent(Graphics graphComprador) {
        graphComprador.setColor(Color.blue);
        graphComprador.drawOval(40,40,100,100);
    }
}
